overview_grid_power_sensor:
  sensor:
  - id: overview_grid_power
    platform: homeassistant
    entity_id: sensor.esp32_s3_energy_meter_grid_power
    on_value:
    - lvgl.label.update:
        id: overview_grid_power_value
        text: !lambda |-
          if (std::isnan(x)) return std::string("--");
          char buf[20];
          snprintf(buf, sizeof(buf), "%+.2f kW", x);
          return std::string(buf);

overview_peak_month_sensor:
  sensor:
  - id: overview_peak_month
    platform: homeassistant
    entity_id: sensor.peak_tarrif_monthly_demand
    on_value:
    - lvgl.label.update:
        id: overview_peak_month_value
        text: !lambda |-
          if (std::isnan(x)) return std::string("--");
          char buf[18];
          snprintf(buf, sizeof(buf), "%.2f kW", x);
          return std::string(buf);

overview_peak_quarter_sensor:
  sensor:
  - id: overview_peak_quarter
    platform: homeassistant
    entity_id: sensor.peak_tarrif_quarterly_demand
    on_value:
    - lvgl.label.update:
        id: overview_peak_quarter_value
        text: !lambda |-
          if (std::isnan(x)) return std::string("--");
          char buf[18];
          snprintf(buf, sizeof(buf), "%.2f kW", x);
          return std::string(buf);

overview_solar_total_sensor:
  sensor:
  - id: overview_solar_total
    platform: homeassistant
    entity_id: sensor.total_ac_power_pv
    on_value:
    - lvgl.label.update:
        id: overview_solar_total_value
        text: !lambda |-
          if (std::isnan(x)) return std::string("--");
          char buf[18];
          snprintf(buf, sizeof(buf), "%.2f kW", x);
          return std::string(buf);

overview_price_sensor:
  sensor:
  - id: overview_price
    platform: homeassistant
    entity_id: sensor.elia_sdac_current_price
    on_value:
    - lvgl.label.update:
        id: overview_price_value
        text: !lambda |-
          if (std::isnan(x)) return std::string("--");
          char buf[20];
          snprintf(buf, sizeof(buf), "%.0f EUR/MWh", x);
          return std::string(buf);

overview_polestar_d_sensor:
  sensor:
  - id: overview_polestar_d
    platform: homeassistant
    entity_id: sensor.polestar_2_frc_521_battery_charge_level
    on_value:
    - lvgl.label.update:
        id: overview_polestar_d_value
        text: !lambda |-
          if (std::isnan(x)) return std::string("--");
          char buf[16];
          snprintf(buf, sizeof(buf), "%.0f%%", x);
          return std::string(buf);

overview_polestar_m_sensor:
  sensor:
  - id: overview_polestar_m
    platform: homeassistant
    entity_id: sensor.polestar_2_fdw_339_battery_charge_level
    on_value:
    - lvgl.label.update:
        id: overview_polestar_m_value
        text: !lambda |-
          if (std::isnan(x)) return std::string("--");
          char buf[16];
          snprintf(buf, sizeof(buf), "%.0f%%", x);
          return std::string(buf);

overview_m1_soc_sensor:
  sensor:
  - id: overview_m1_soc
    platform: homeassistant
    entity_id: sensor.marstek_m1_battery_state_of_charge
    on_value:
    - lvgl.label.update:
        id: overview_m1_soc_value
        text: !lambda |-
          if (std::isnan(x)) return std::string("--");
          char buf[16];
          snprintf(buf, sizeof(buf), "%.0f%%", x);
          return std::string(buf);

overview_m2_soc_sensor:
  sensor:
  - id: overview_m2_soc
    platform: homeassistant
    entity_id: sensor.marstek_m2_battery_state_of_charge
    on_value:
    - lvgl.label.update:
        id: overview_m2_soc_value
        text: !lambda |-
          if (std::isnan(x)) return std::string("--");
          char buf[16];
          snprintf(buf, sizeof(buf), "%.0f%%", x);
          return std::string(buf);

overview_m3_soc_sensor:
  sensor:
  - id: overview_m3_soc
    platform: homeassistant
    entity_id: sensor.marstek_m3_battery_state_of_charge
    on_value:
    - lvgl.label.update:
        id: overview_m3_soc_value
        text: !lambda |-
          if (std::isnan(x)) return std::string("--");
          char buf[16];
          snprintf(buf, sizeof(buf), "%.0f%%", x);
          return std::string(buf);
